<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Dashboard — DevConnect</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <style>
        * { margin:0; padding:0; box-sizing:border-box; }
        html, body { height:100%; }
        body {
            background:#0a0a0a;
            color:#e0e0e0;
            font-family:'Inter', sans-serif;
            display:flex;
            flex-direction:column;
            min-height:100vh;
        }
        main { flex:1; }
        .container { max-width:820px; margin:48px auto; padding:0 24px; }
        h1 { font-size:2rem; font-weight:800; background:linear-gradient(90deg,#d4a017,#2e8b57); -webkit-background-clip:text; -webkit-text-fill-color:transparent; margin-bottom:6px; }
        .subtitle { color:#808080; margin-bottom:36px; font-size:0.97rem; }
        .post-card {
            background:linear-gradient(135deg,#111111 0%,#1a1a1a 100%);
            border-radius:14px;
            padding:26px 28px;
            margin-bottom:20px;
            border:1px solid #2a2a2a;
            transition:border-color 0.25s, box-shadow 0.25s, transform 0.2s;
        }
        .post-card:hover { border-color:#d4a01788; box-shadow:0 4px 32px #d4a01718; transform:translateY(-2px); }
        .post-card h2 { color:#e8e8e8; margin-bottom:8px; font-size:1.15rem; font-weight:700; }
        .post-card h2 a { color:#e8e8e8; text-decoration:none; transition:color 0.2s; }
        .post-card h2 a:hover { color:#2e8b57; }
        .post-meta { color:#707070; font-size:0.82rem; margin-bottom:10px; }
        .post-excerpt { color:#b0b0b0; line-height:1.65; font-size:0.95rem; }
        .tag { display:inline-block; background:#1a1a0a; color:#d4a017; border-radius:6px; padding:2px 10px; font-size:0.76rem; font-weight:600; margin-bottom:10px; }
        .empty { text-align:center; color:#606060; padding:80px 0; }
        .empty a { color:#d4a017; text-decoration:none; font-weight:600; }
        .empty a:hover { color:#2e8b57; }
        .write-btn {
            display:inline-flex; align-items:center; gap:8px;
            background:linear-gradient(135deg,#d4a017,#2e8b57);
            color:white; padding:10px 24px; border-radius:10px;
            text-decoration:none; font-weight:700; font-size:0.92rem;
            margin-bottom:32px; box-shadow:0 2px 14px #d4a01733;
            transition:opacity 0.2s, transform 0.2s;
        }
        .write-btn:hover { opacity:0.88; transform:translateY(-1px); }
    </style>
</head>
<body>
    <%- include('../partials/navbar') %>

    <main>
        <div class="container">
            <h1>Latest Posts</h1>
            <p class="subtitle">Welcome back, <strong><%= user.name %></strong> 👋</p>

            <a href="/post/create" class="write-btn">✏️ Write a Post</a>

            <% if (posts.length === 0) { %>
                <div class="empty">
                    <p>No posts yet. <a href="/post/create">Write the first one!</a></p>
                </div>
            <% } else { %>
                <% posts.forEach(post => { %>
                    <div class="post-card">
                        <span class="tag">Article</span>
                        <h2><a href="/post/<%= post._id %>"><%= post.title %></a></h2>
                        <div class="post-meta">
                            By <strong><%= post.author.name %></strong> &nbsp;•&nbsp;
                            <%= new Date(post.createdAt).toDateString() %>
                        </div>
                        <p class="post-excerpt"><%= post.content.substring(0, 160) %>...</p>
                    </div>
                <% }) %>
            <% } %>
        </div>
    </main>

    <%- include('../partials/footer') %>
</body>
</html>